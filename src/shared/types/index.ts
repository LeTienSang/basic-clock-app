// shared/types/index.ts — TypeScript types/interfaces toàn cục

/** Theme của ứng dụng */
export type Theme = 'light' | 'dark';

/** Tab điều hướng chính */
export type ActiveTab = 'clock' | 'timer' | 'stopwatch';
